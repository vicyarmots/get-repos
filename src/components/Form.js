import React from 'react';
import { Header, SearchForm, ListRepository } from '../common/index';

const Form = () => {
    return (
        <div className="form">
            <Header />
            <SearchForm />
            <ListRepository />
        </div>
    );
};

export default Form;
